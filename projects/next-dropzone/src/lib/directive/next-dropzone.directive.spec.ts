import { NextDropzoneDirective } from './next-dropzone.directive';

describe('NextDropzoneDirective', () => {
  it('should create an instance', () => {
    const directive = new NextDropzoneDirective();
    expect(directive).toBeTruthy();
  });
});
